import React, { ReactElement } from "react";
import { Header } from "./Header";

export const AboutSection = (): ReactElement => {
  return (
    <section className="grid grid-cols-1 w-full p-10 text-primary-color"></section>
  );
};
